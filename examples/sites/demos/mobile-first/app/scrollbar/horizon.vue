<template>
  <tiny-scrollbar style="width: 400px">
    <div style="width: 600px">
      <div>
        当内部元素大于外部元素宽高时，出现滚动条当内部元素大于外部元素宽高时，出现滚动条元素大于外部元素宽高时，出现滚动条
      </div>
      <div>
        当内部元素大于外部元素宽高时，出现滚动条当内部元素大于外部元素宽高时，出现滚动条元素大于外部元素宽高时，出现滚动条
      </div>
      <div>
        当内部元素大于外部元素宽高时，出现滚动条当内部元素大于外部元素宽高时，出现滚动条元素大于外部元素宽高时，出现滚动条
      </div>
      <div>
        当内部元素大于外部元素宽高时，出现滚动条当内部元素大于外部元素宽高时，出现滚动条元素大于外部元素宽高时，出现滚动条
      </div>
      <div>
        当内部元素大于外部元素宽高时，出现滚动条当内部元素大于外部元素宽高时，出现滚动条元素大于外部元素宽高时，出现滚动条
      </div>
    </div>
  </tiny-scrollbar>
</template>

<script>
import { Scrollbar } from '@opentiny/vue'

export default {
  components: {
    TinyScrollbar: Scrollbar
  }
}
</script>

import { THEMES } from '../../constants';
import getToken from '../../factory';

const bpitLight = getToken(THEMES.BPIT_LIGHT);

const bpitDark = getToken(THEMES.BPIT_DARK);

export { bpitLight, bpitDark };

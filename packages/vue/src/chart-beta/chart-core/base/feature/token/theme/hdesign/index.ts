import { THEMES } from '../../constants';
import getToken from '../../factory';

const hdesignLight = getToken(THEMES.HDESIGN_LIGHT);

const hdesignDark = getToken(THEMES.HDESIGN_DARK);

export { hdesignLight, hdesignDark };

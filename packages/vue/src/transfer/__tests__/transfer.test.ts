import { mountPcMode } from '@opentiny-internal/vue-test-utils'
import { describe, test } from 'vitest'

describe('PC Mode', () => {
    const mount = mountPcMode

    // 数据源
    test.todo('data')

    // 绑定值
    test.todo('modelValue')

    // 是否可搜索，默认值为 true
    test.todo('filterable')

    // 搜索框占位符，默认值为 请输入搜索内容
    test.todo('filter-placeholder')

    // 设置穿梭框的搜索过滤字段
    test.todo('filter-method')

    // 自定义列表标题
    test.todo('titles')

    // 自定义按钮文案
    test.todo('button-texts')

    // 自定义数据项渲染函数
    test.todo('render-content')

    // 列表顶部勾选状态文案
    test.todo('format')

    // 数据源的字段别名
    test.todo('props')

    // 初始状态下左侧列表的已勾选项的 key 数组
    test.todo('left-default-checked')

    // 初始状态下右侧列表的已勾选项的 key 数组
    test.todo('right-default-checked')

    // 右侧列表元素的排序策略
    test.todo('target-order')
    
    // 穿梭至左侧面板的按钮禁用
    test.todo('to-left-disable')
    
    // 穿梭至右侧面板的按钮禁用
    test.todo('to-right-disable')
    
    // 展示全部移动按钮
    test.todo('show-all-btn')
    
    // 设置穿梭框列表项可拖拽
    test.todo('drop-config')
    
    // 设置穿梭框的表格列配置
    test.todo('columns')
    
    // 设置分页配置
    test.todo('page-vo')
    
    // 设置是否显示分页，当渲染为表格时有效
    test.todo('show-pager')
    
    // 设置分页配置，当渲染为表格时有效
    test.todo('pager-op')
    
    // 当plugin设置为Table时设置渲染为表格;当plugin设置为Tree渲染为树
    test.todo('render')
    
    // 当plugin设置为Tree渲染为树时，设置树的相关配置属性
    test.todo('tree-op')
})
